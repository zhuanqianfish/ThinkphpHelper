前期准备：
准备一个全新的thinkphp5项目，运行目录为/public,数据库配置已经写好，
实际使用时可以使用任意数据库

1.复制tph2文件夹到项目运行目录，也就是public目录下

1.5 访问一下http://[项目域名] 看到thinkphp5的默认首页，这一步是为了生成index模块相应的文件，
  以下均使用index模块作为目标模块，实际使用中可以根据需要自己创建模块文件）
	

2.访问http://[项目域名]/tph2  
（如果提示“No input file specified.”，需要修改public目录下的.haccess,
	把最后一行改成RewriteRule ^(.*)$ index.php?s=$1 [QSA,PT,L]  ）

3.生成公共文件
4.生成菜单
5.生成前台界面
6.生成控制器
7.使用简单模式生成模型代码

8.访问index模块下对应的方法，如 http://[项目域名]/index/admin/getList



